pipeline {
    agent any

    stages {
        stage("compile") {
            steps {
                sh 'Hello World'
            }
        }

        stage("run") {
            steps {
                sh 'Hello World'
            }
        }
    }
}
